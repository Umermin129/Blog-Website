import React from 'react';
import {
  Button,
  Table,
  TableHead,
  TableRow,
  TableCell,
  TableBody,
  styled
} from '@mui/material';
import { Link, useSearchParams } from 'react-router-dom';

// Other necessary imports...

const StyledTable = styled(Table)`
  border: 1px solid rgba(224, 224, 224, 1);
`;

// Styled components and Categories component...

const Categories = () => {
  // Logic to display categories with links...

  return (
    <>
      {/* Display categories and create blog button */}
    </>
  );
};

export default Categories;
